# Project Rules

- **Media Additions**: Whenever new images or videos are added to the `Denis/img/` directory, always integrate them into both:
  1. The corresponding static chapter grid in `Denis/index.html`.
  2. The slideshow playlist in `Denis/script.js` (including setting a natural, context-aware Hebrew caption).
